package de.unisaarland.cs.se.selab.systemtest.amitSystemTests

/*
class BerlinSimple : SystemTest() {

    override val name = "Berlin Simple Test"

    override val map = "mapFiles/Berlin.dot"
    override val assets = "assetsJsons/berlin_assets.json"
    override val scenario = "scenarioJsons/berlin_scenario1.json"
    override val maxTicks = 15

    override suspend fun run() {
        assertNextLine("Initialization Info: Berlin.dot successfully parsed and validated")
        assertNextLine("Initialization Info: berlin_assets.json successfully parsed and validated")
        assertNextLine("Initialization Info: berlin_scenario1.json successfully parsed and validated")

        assertNextLine("Simulation starts")

        assertNextLine("Simulation Tick: 0")
        assertNextLine("Simulation Tick: 1")

        assertNextLine("Emergency Assignment: 0 assigned to 4")
        assertNextLine("Asset Allocation: 18 allocated to 0; 1 ticks to arrive.")
        assertNextLine("Asset Allocation: 19 allocated to 0; 1 ticks to arrive.")

        assertNextLine("Simulation Tick: 2")

        assertNextLine("Asset Arrival: 18 arrived at 30.")
        assertNextLine("Asset Arrival: 19 arrived at 30.")

        assertNextLine("Emergency Handling Start: 0 handling started.")
        assertNextLine("Event Triggered: 3 triggered.")

        assertNextLine("Simulation Tick: 3")
        assertNextLine("Simulation Tick: 4")
        assertNextLine("Simulation Tick: 5")

        assertNextLine("Emergency Resolved: 0 resolved.")

        assertNextLine("Simulation End")

        assertNextLine("Simulation Statistics: 0 assets rerouted.")
        assertNextLine("Simulation Statistics: 1 received emergencies.")
        assertNextLine("Simulation Statistics: 0 ongoing emergencies.")
        assertNextLine("Simulation Statistics: 0 failed emergencies.")
        assertNextLine("Simulation Statistics: 1 resolved emergencies.")

        assertEnd()
    }
}
*/
