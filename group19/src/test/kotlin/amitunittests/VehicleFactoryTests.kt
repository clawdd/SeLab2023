

class VehicleFactoryTests {
    /*
    private val factory = VehicleFactory()

    @Test
    fun testPoliceCar() {
        val p: PoliceVehicle = factory.createPoliceCar(24, null, 2, 2, 20)

        assertEquals(VehicleType.POLICE_CAR, p.vehicleType, "Incorrect Vehicle Type")
        assertEquals(2, p.staffCapacity, "Incorrect staff nos in Factory!")
        assertEquals(24, p.vehicleId, "Incorrect attribute value!")
        assertEquals(2, p.criminalCapacity, "Incorrect attribute value!")
        assertNull(p.homeBaseId, "Base should be null!")
        assertEquals(20, p.vehicleHeight, "Vehicle Factory created incorrect height!")
    }
    @Test
    fun testPoliceK9Car() {
        val p: PoliceVehicle = factory.createPoliceK9Car(20, null, 2, 2)

        assertEquals(VehicleType.K9_POLICE_CAR, p.vehicleType, "Incorrect Vehicle Type")
        assertEquals(2, p.staffCapacity, "Incorrect staff nos in Factory!")
        assertEquals(20, p.vehicleId, "Incorrect attribute value!")
        assertNull(p.homeBaseId, "Base should be null!")
        assertEquals(2, p.vehicleHeight, "Vehicle Factory created incorrect height!")
    }
    @Test
    fun testPoliceMotorcycle() {
        val p: PoliceVehicle = factory.createPoliceMotorcycle(25, null, 2, 2)

        assertEquals(VehicleType.POLICE_MOTORCYCLE, p.vehicleType, "Incorrect Vehicle Type")
        assertEquals(2, p.staffCapacity, "Incorrect staff nos in Factory!")
        assertEquals(25, p.vehicleId, "Incorrect attribute value!")
        assertNull(p.homeBaseId, "Base should be null!")
        assertEquals(2, p.vehicleHeight, "Vehicle Factory created incorrect height!")
    }
    @Test
    fun testFireWater() {
        val p: FireVehicle = factory.createFireWater(100, null, 2, 2, 34)

        assertEquals(VehicleType.FIRE_TRUCK_WATER, p.vehicleType, "Incorrect Vehicle Type")
        assertEquals(2, p.staffCapacity, "Incorrect staff nos in Factory!")
        assertEquals(100, p.vehicleId, "Incorrect attribute value!")
        assertNull(p.homeBaseId, "Base should be null!")
        assertEquals(34, p.vehicleHeight, "Vehicle Factory created incorrect height!")
        assertEquals(2, p.waterCapacity, "Vehicle Factory created incorrect water capacity!")
    }
    @Test
    fun testFireTechnical() {
        val p: FireVehicle = factory.createFireTechnical(100, null, 2, 2)

        assertEquals(VehicleType.FIRE_TRUCK_TECHNICAL, p.vehicleType, "Incorrect Vehicle Type")
        assertEquals(2, p.staffCapacity, "Incorrect staff nos in Factory!")
        assertEquals(100, p.vehicleId, "Incorrect attribute value!")
        assertNull(p.homeBaseId, "Base should be null!")
        assertEquals(2, p.vehicleHeight, "Vehicle Factory created incorrect height!")
        assertEquals(0, p.waterCapacity, "Vehicle Factory created incorrect default water capacity!")
    }
    @Test
    fun testFireLadder() {
        val p: FireVehicle = factory.createFireLadder(100, null, 2, 2, 25)

        assertEquals(VehicleType.FIRE_TRUCK_LADDER, p.vehicleType, "Incorrect Vehicle Type")
        assertEquals(2, p.staffCapacity, "Incorrect staff nos in Factory!")
        assertEquals(100, p.vehicleId, "Incorrect attribute value!")
        assertNull(p.homeBaseId, "Base should be null!")
        assertEquals(2, p.ladderLength, "Vehicle Factory created incorrect height!")
        assertEquals(25, p.vehicleHeight, "Vehicle Factory created incorrect default water capacity!")
    }
    @Test
    fun testFireTransporter() {
        val p: FireVehicle = factory.createFireTransporter(100, null, 2, 2)

        assertEquals(VehicleType.FIREFIGHTER_TRANSPORTER, p.vehicleType, "Incorrect Vehicle Type")
        assertEquals(2, p.staffCapacity, "Incorrect staff nos in Factory!")
        assertEquals(100, p.vehicleId, "Incorrect attribute value!")
        assertNull(p.homeBaseId, "Base should be null!")
        assertEquals(2, p.vehicleHeight, "Vehicle Factory created incorrect default water capacity!")
    }
    @Test
    fun testHospitalAmbulance() {
        val p: MedicalVehicle = factory.createHospitalAmbulance(100, null, 2, 2)

        assertEquals(VehicleType.AMBULANCE, p.vehicleType, "Incorrect Vehicle Type")
        assertEquals(2, p.staffCapacity, "Incorrect staff nos in Factory!")
        assertEquals(100, p.vehicleId, "Incorrect attribute value!")
        assertNull(p.homeBaseId, "Base should be null!")
        assertEquals(2, p.vehicleHeight, "Vehicle Factory created incorrect default water capacity!")
    }
    @Test
    fun testHospitalDoctorCar() {
        val p: MedicalVehicle = factory.createHospitalDoctorCar(100, null, 2, 2)

        assertEquals(VehicleType.EMERGENCY_DOCTOR_CAR, p.vehicleType, "Incorrect Vehicle Type")
        assertEquals(2, p.staffCapacity, "Incorrect staff nos in Factory!")
        assertEquals(100, p.vehicleId, "Incorrect attribute value!")
        assertNull(p.homeBaseId, "Base should be null!")
        assertEquals(2, p.vehicleHeight, "Vehicle Factory created incorrect default water capacity!")
    }
    */
}
